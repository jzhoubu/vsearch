max_len: 256
dropout: 0.1
shared_encoder: False
device: 

encoder_q:
  type: dpr
  max_len: 128
  base_model_id: bert-base-uncased

encoder_p:
  type: dpr
  max_len: 256
  base_model_id: bert-base-uncased
